#!/usr/bin/env bash
# Enable with: git config core.hooksPath hooks
# Blocks a commit if any vault note fails tools/lint.py.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
python3 tools/lint.py --path vault
